import { MesssagesCategoryFilterPipe } from './messsages-category-filter.pipe';

describe('MesssagesCategoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MesssagesCategoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
